# 🌱 EcoLearn - Environmental Education Platform

An interactive learning platform focused on environmental education with gamified lessons, AR experiences, and student progress tracking.

## 📁 Project Structure

```
EcoLearn/
├── Frontend/          # React + Vite frontend (deployed on Render)
│   ├── src/
│   │   ├── pages/
│   │   ├── components/
│   │   ├── services/
│   │   └── ...
│   └── package.json
│
└── Backend/          # Node.js + Express API
    ├── src/
    │   ├── controllers/
    │   ├── models/
    │   ├── routes/
    │   └── server.js
    └── package.json
```

## 🚀 Quick Start

### Frontend (Local Development)

```bash
cd Frontend
npm install
npm run dev
```

The frontend will run on `http://localhost:5173`

### Backend (Local Development)

```bash
cd Backend
npm install
npm run dev
```

The backend will run on `http://localhost:5000`